/* let letra = "a";
let vogal = ["a", "e", "i", "o", "u"];

if (letra === vogal){
    console.log(`vogal`)

}else if (letra != vogal) {
    console.log(`consoante`)
}

console.log(`${letra}`)
/* ESSE NAO CONSEGUI FAZER */ 
let letra = "a";

if (letra === 'a' || letra === 'e' || letra === 'i' || letra === 'o' || letra === 'u') {
    console.log(`vogal`);
} else {
    console.log(`consoante`);
}

console.log(`${letra}`);

console.log(`------------------------------------------------------------------`)

let L = "z";
let vogais = ["a", "e", "i", "o", "u"];

if (vogais.includes(L)) {
    console.log(`vogal`);
} else {
    console.log(`consoante`);
}

console.log(`${L}`);

/* tentei de 3 jeitos diferentes */
